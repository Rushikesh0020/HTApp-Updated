class NextEvents {
  String releaseTitle, thumbnail, category, description, url;
  NextEvents({
    required this.releaseTitle,
    required this.category,
    required this.thumbnail,
    required this.description,
    required this.url
  });
}
